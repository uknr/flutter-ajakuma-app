name: flutter_application_ajakuma
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  flutter_animate: ^4.2.0
  google_fonts: ^4.0.4
  cupertino_icons: ^1.0.8
  logger: ^2.0.2 # バージョンは最新のものを使ってください
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
